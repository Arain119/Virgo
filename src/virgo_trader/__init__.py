"""Virgo Trader core package.

This project is organized as a Python package so the CLI entrypoints and the
desktop UI can share the same implementation (training, backtesting, environment, utilities).
"""
